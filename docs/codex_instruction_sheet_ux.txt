「柿川亭・アブララボ 会計集計」スプレッドシート（ID: 1UXr4m54rClrJknIn_o3DRupMQbYRriSXmykAkJkQx90）の入力UXを改善したい。
以下をStep順に実装してください。既存の数式・データは壊さないこと。各Stepごとにmasaが確認してから次のStepに進める想定です。

【Step1：タブ移動の負担軽減】
1. 全ての入力系タブ（日次_柿川亭／日次_アブララボ／メニュー_柿川亭／仕入経費／人件費）で、見出し行の固定（表示→固定→1行）と先頭列（日付列）の固定を設定する。
2. 手入力セルの背景色を #fff6c8、自動計算セル（数式が入っているセル）の背景色を #e3f3e1・文字色 #5c5c5c に、全タブで統一する。
3. タブの並び順を「入力ハブ／日次_柿川亭／日次_アブララボ／メニュー_柿川亭／仕入経費／人件費／設定／使い方／月次集計／メニュー_アブララボ」の順に変更する。
4. タブ名を次の通り変更する：「📋入力ハブ」「🍜日次:柿川亭」「🧴日次:アブラボ」「🍜メニュー:柿川亭」「🧾仕入経費」「👤人件費」（設定・使い方・月次集計・メニュー_アブララボは名称そのままでよい）。
5. タブの色を設定：入力ハブ=紺、柿川亭関連（日次・メニュー）=青（#2a78d6系）、アブララボ関連=オレンジ（#eb6834系）、仕入経費・人件費=グレー。
6. 設定／使い方／月次集計／メニュー_アブララボの4タブを非表示にする（シートを右クリック→非表示）。データは消さない。
7. 新規タブ「📋入力ハブ」を先頭に作成し、以下を配置する：
   - 今日の日付（=TODAY()）
   - 日次_柿川亭／日次_アブララボ／メニュー_柿川亭／仕入経費／人件費、それぞれについて「今日の日付の行が入力済みか」を判定するチェックセル（該当シートの今日日付行の主要項目が空でなければ✅、空なら⬜）
   - 各行に「▶開く」ボタン（=HYPERLINK関数で該当タブへジャンプ）
   - 設定タブの対象月と今日の月が一致しない場合に警告文を表示するセル（=IF(MONTH(TODAY())<>設定タブの対象月セル, "⚠設定タブの対象月を更新してください", "")）

【Step2：仕入経費タブの税区分・税率を統合（最優先）】
1. 仕入経費タブに新しい列「税区分・税率」を、既存の「税区分」列の左（仕入先・費目列の右）に追加する。
2. この列にデータの入力規則でプルダウンリストを設定：選択肢は「税込8%」「税込10%」「税抜8%」「税抜10%」の4つのみ。
3. 既存の「税区分」列の中身を数式に置き換える：=IF(LEFT(税区分・税率セル,2)="税込","税込","税抜")
4. 既存の「税率」列の中身を数式に置き換える：=IF(RIGHT(税区分・税率セル,3)="10%",10%,8%)
   ※税抜金額／消費税／税込金額の計算式はこの2列を参照したままで変更不要。
5. 上記2つの列を自動計算セルの見た目（緑背景・グレー文字）に変更し、警告付きで保護する。
6. 「税区分・税率」列のヘッダーセルにノート（コメント）を追加：
   「税込8%＝食品・軽減税率のレシート／税込10%＝標準税率のレシート（内税表記）／税抜8%・税抜10%＝業者の請求書等で税抜金額が別記されている場合。8%か10%か迷ったら食品・飲料（酒類・外食除く）＝8%、それ以外＝10%が目安。」
7. 設定タブに「仕入先ごとの既定税区分・税率」の対応表（仕入先名／既定の税区分・税率の2列）を追加する。初期値は以下を仮設定し、実際の運用に合わせてmasaに確認・修正してもらう：
   めんつう=税込8%／ホクショク=税込8%／中越鶏卵=税込8%／マルヤマ包装=税抜10%／竹内農園=税込8%／アルベーロ=税込8%／スーパー=税込8%／北陸ガス=税抜10%／東北電力=税抜10%／その他=空欄
8. 仕入経費タブに「提案（参考）」列を追加し、=VLOOKUP(仕入先・費目セル, 設定タブの対応表, 2, FALSE) で仕入先ごとの既定パターンを表示する。
9. 条件付き書式を設定：
   - 「税区分・税率」列が空欄なのに「入力額」列に値がある行 → 背景を赤系（#fde0df）にする
   - 「税区分・税率」列の選択値が「提案（参考）」列の値と異なる行 → 背景を薄いオレンジにする
   - 税込金額 ≠ 税抜金額＋消費税 になっている行 → 文字を赤・太字にする

【Step3：ドロップダウン化】
1. 仕入経費タブ「仕入先・費目」列を自由入力からプルダウンに変更（リスト元＝設定タブの仕入先リスト）。
2. 仕入経費タブに「支払方法」列を追加し、プルダウン（現金／カード／振込／その他）を設定。
3. 設定タブに「スタッフ名簿」（氏名・時給の一覧）を追加し、人件費タブの「氏名」列をこのリストからのプルダウンに変更。
4. 全タブの日付列にデータの入力規則「日付」を設定する。
5. メニュー_柿川亭の各メニュー列に、データの入力規則で「0以上の整数」を強制する。

【Step4：条件付き書式でのエラー検知】
1. 日次_柿川亭／日次_アブララボの「差額確認」列：0以外の値がある行は背景赤・太字にする。ヘッダーにノート「日次の券売機売上（封筒記載）とメニュー_柿川亭の券売機売上（レシート集計）の差。0以外は封筒かレシートの読み取りミスの可能性」を追加する。
2. 各入力タブで、今日の日付に該当する行の必須項目（店舗別に主要列を指定）が空欄の場合、該当セルを薄いグレーにして「未入力」を視覚化する。

【Step5：日付の逆時系列化（要事前確認のうえ実施）】
1. 日次_柿川亭／日次_アブララボ／メニュー_柿川亭／仕入経費／人件費の各タブで、データ行を日付の新しい順（降順）に並び替える。
2. 各タブの「月次小計」相当行を、見出し行のすぐ下（2行目）に移動し、見出し行と合わせて2行固定にする。
3. 実施前に、月次集計タブおよびジョー側の集計ロジックが行の位置や順序に依存していないか確認する（ヘッダー名参照であれば影響なしのはず）。

【Step6：仕上げ】
1. 日次_柿川亭の「券売機杯数」列を手入力から自動計算に変更：=VLOOKUP(その行の日付, メニュー_柿川亭の日付列:純販売数合計列, 該当列番号, FALSE)。手入力ではなくなるため列の色も緑に変更し保護する。
2. メニュー_柿川亭の列を「頻出順」に並べ替え（並/大/特盛りなど主力商品を左側、季節商品・ギフト等を右側）、トッピング・ドリンク・季節商品を列のグループ化（アウトライン）でまとめ、デフォルトで折りたたんでおく。
3. 全タブの金額セルの数値書式を ¥#,##0 に統一する。
4. 全タブ右上に🟡入力／🟢自動／🔴要確認の凡例ミニ表を設置する。
5. 自動計算セル全般（売上合計、換算人数各種、推定来店客数、消費税額、税込金額、勤務時間、人件費、純販売数合計、月次集計タブ全体）に警告付きの保護をかける。

各Stepの完了後、変更点のスクリーンショットか一覧をmasaに共有し、確認を取ってから次のStepに進んでください。
